import { NbGridItemDirective } from './nb-grid-item.directive';

describe('NbGridItemDirective', () => {
  it('should create an instance', () => {
    const directive = new NbGridItemDirective();
    expect(directive).toBeTruthy();
  });
});
