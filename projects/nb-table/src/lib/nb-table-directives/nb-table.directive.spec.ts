import { NbTableDirective } from './nb-table.directive';

describe('NbTableDirective', () => {
  it('should create an instance', () => {
    // const directive = new NbTableDirective();
    // expect(directive).toBeTruthy();
  });
});
