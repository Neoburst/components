import { NbInputDirective } from './nb-input.directive';

describe('NbInputDirective', () => {
  it('should create an instance', () => {
    const directive = new NbInputDirective();
    expect(directive).toBeTruthy();
  });
});
